# Generates NPC dialogue lines given scene context.

def generate(npc_id: str, context: str) -> dict:
    # TODO: prompt LLM with NPC persona + context
    return {"npc_id": npc_id, "line": "Coming Soon"}
