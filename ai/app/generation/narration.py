# Generates narration text for a given scene/story node.

def generate(scene_id: str) -> dict:
    # TODO: prompt LLM with scene/story context
    return {"scene_id": scene_id, "narration": "Coming Soon"}
