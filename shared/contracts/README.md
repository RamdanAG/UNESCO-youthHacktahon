# Shared API Contracts

Placeholder for shared type contracts between frontend and backend
(e.g. generated from FastAPI's OpenAPI schema via `openapi-typescript`).

TODO: add generation script in /scripts to keep these in sync.
